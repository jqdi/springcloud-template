package ${package.Controller};

import org.springframework.web.bind.annotation.*;
import org.springframework.beans.factory.annotation.Autowired;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;

import ${package.Entity}.${entity};
import ${package.Service}.${entity}Feign;
import ${package.Service}.$!table.serviceName;

/**
 * ${table.comment} 前端控制器
 *
 * @author CodeGenerate
 */
@Api(tags = "$!table.comment管理")
@RestController
@RequestMapping("/${entity.substring(0,1).toLowerCase()}${entity.substring(1)}")
public class ${table.controllerName}  implements ${entity}Feign {
    
    @Autowired
    private $!table.serviceName ${table.serviceName.substring(0,1).toLowerCase()}${table.serviceName.substring(1)};

	private QueryWrapper<${entity}> toQueryWrapper(String name, String code, String value, String configRemark) {
		QueryWrapper<${entity}> queryWrapper = new QueryWrapper<>();
		if (StringUtils.isNotBlank(name)) {
			queryWrapper.like("name", name);
		}
		if (StringUtils.isNotBlank(code)) {
			queryWrapper.like("code", code);
		}
		if (StringUtils.isNotBlank(value)) {
			queryWrapper.like("value", value);
		}
		if (StringUtils.isNotBlank(configRemark)) {
			queryWrapper.like("config_remark", configRemark);
		}
		return queryWrapper;
	}

    /**
     * 分页查询${table.comment}
     */
    @ApiOperation("分页查询${table.comment}")
    @GetMapping("/page")
    public Page<${package.Entity}.${entity}> page(
        @RequestParam(defaultValue = "1") Long current,
        @RequestParam(defaultValue = "10") Long size) {
        Page<${package.Entity}.${entity}> page = new Page<>(current, size);
        return ${table.serviceName.substring(0,1).toLowerCase()}${table.serviceName.substring(1)}.page(page);
    }
    
    /**
     * 根据ID查询${table.comment}
     */
    @ApiOperation("根据ID查询${table.comment}")
    @GetMapping("/{id}")
    public ${package.Entity}.${entity} get(@PathVariable Integer id) {
        return ${table.serviceName.substring(0,1).toLowerCase()}${table.serviceName.substring(1)}.getById(id);
    }
    
    /**
     * 保存${table.comment}
     */
    @ApiOperation("保存${table.comment}")
    @PostMapping("/save")
    public Boolean save(@RequestBody ${package.Entity}.${entity} ${entity.substring(0,1).toLowerCase()}${entity.substring(1)}) {
        return ${table.serviceName.substring(0,1).toLowerCase()}${table.serviceName.substring(1)}.save(${entity.substring(0,1).toLowerCase()}${entity.substring(1)});
    }
    
    /**
     * 更新${table.comment}
     */
    @ApiOperation("更新${table.comment}")
    @PostMapping("/update")
    public Boolean update(@RequestBody ${package.Entity}.${entity} ${entity.substring(0,1).toLowerCase()}${entity.substring(1)}) {
        return ${table.serviceName.substring(0,1).toLowerCase()}${table.serviceName.substring(1)}.updateById(${entity.substring(0,1).toLowerCase()}${entity.substring(1)});
    }
    
    /**
     * 删除${table.comment}
     */
    @ApiOperation("删除${table.comment}")
    @PostMapping("/remove")
    public Boolean remove(@RequestBody Integer id) {
        return ${table.serviceName.substring(0,1).toLowerCase()}${table.serviceName.substring(1)}.removeById(id);
    }
}
